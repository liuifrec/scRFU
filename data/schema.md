# Data Schema

## km5000_centroids.tsv.gz

This is the RFU atlas file.

Expected columns depend on the RFU implementation, but should minimally
include:

-   rfu_label : RFU cluster label
-   centroid features or embedding dimensions

The R implementation defines the authoritative schema.
